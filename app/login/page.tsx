import React, { FC } from "react";

// The Login includes a form for login into todo app
const Login: FC = () => {
  return <div className="text-sm text-center">Login Page</div>;
};

export default Login;
