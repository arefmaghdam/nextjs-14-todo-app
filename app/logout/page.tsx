import React, { FC } from "react";

// The Logout make it that you can logout from todo app account
const Logout: FC = () => {
  return <div className="text-sm text-center">Logout Page</div>;
};

export default Logout;
